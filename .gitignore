.claude/
*.log
Thumbs.db
.DS_Store

# Draft article documents — source material, not deployed
*.docx

# Original unslugified blog hero images kept at the repo root.
# The deployed copies live in images/blogs/ with slugified names.
/*.webp
